@model GrupoMad.Models.PriceList

@{
    ViewData["Title"] = "Gestionar Productos";
}

<h1>Gestionar Productos - @Model.Name</h1>

@if (Model.Store != null)
{
    <h5 class="text-muted">Tienda: @Model.Store.Name</h5>
}
else
{
    <h5 class="text-muted">Lista Global</h5>
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["Info"] != null)
{
    <div class="alert alert-info alert-dismissible fade show" role="alert">
        @TempData["Info"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@* Mostrar información si la lista está vinculada a un ProductType *@
@if (Model.ProductTypeId.HasValue && Model.ProductType != null)
{
    <div class="alert alert-info">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <i class="bi bi-link-45deg"></i>
                <strong>Lista vinculada al tipo de producto:</strong> @Model.ProductType.Name
                <br />
                <small class="text-muted">
                    • Productos nuevos de tipo "@Model.ProductType.Name" se agregan automáticamente<br />
                    • Usa "Sincronizar" para agregar productos faltantes y <strong>remover productos que ya no pertenecen</strong> a este tipo
                </small>
            </div>
            <form asp-action="SyncProducts" method="post" class="d-inline">
                @Html.AntiForgeryToken()
                <input type="hidden" name="priceListId" value="@Model.Id" />
                <button type="submit" class="btn btn-primary" title="Sincroniza la lista: agrega productos nuevos y remueve productos que cambiaron de tipo">
                    <i class="bi bi-arrow-repeat"></i> Sincronizar Ahora
                </button>
            </form>
        </div>
    </div>
}

@* Mostrar opción de porcentaje para listas de tienda (no globales) *@
@if (Model.StoreId.HasValue && Model.ProductTypeId.HasValue)
{
    <div class="card mb-3">
        <div class="card-header bg-warning text-dark">
            <h5 class="mb-0">
                <i class="bi bi-percent"></i> Ajustar Precios desde Lista Global
            </h5>
        </div>
        <div class="card-body">
            <p class="text-muted">
                Los precios de esta lista se basan en la lista global "<strong>Lista de @Model.ProductType?.Name</strong>".
                Aplica un porcentaje para ajustar todos los precios automáticamente.
            </p>
            <form asp-action="ApplyPercentageFromGlobal" method="post" class="row g-3">
                @Html.AntiForgeryToken()
                <input type="hidden" name="priceListId" value="@Model.Id" />

                <div class="col-auto">
                    <label for="percentage" class="form-label">Porcentaje de Ajuste</label>
                    <div class="input-group">
                        <input type="number"
                               class="form-control"
                               id="percentage"
                               name="percentage"
                               step="0.01"
                               placeholder="Ej: 15 o -10"
                               required
                               style="width: 120px;" />
                        <span class="input-group-text">%</span>
                    </div>
                    <small class="form-text text-muted">
                        Positivo para aumentar, negativo para reducir
                    </small>
                </div>

                <div class="col-auto d-flex align-items-end">
                    <button type="submit" class="btn btn-warning" onclick="return confirm('¿Está seguro de aplicar este porcentaje a TODOS los productos de la lista?');">
                        <i class="bi bi-calculator"></i> Aplicar Porcentaje
                    </button>
                </div>
            </form>
        </div>
    </div>
}

<hr />

<div>
    <h3>Productos en la Lista</h3>

    @if (Model.PriceListItems != null && Model.PriceListItems.Any())
    {
        @if (Model.ProductType?.HasVariants == true && Model.ProductType.ProductTypeVariants.Any())
        {
            var productsGrouped = Model.PriceListItems
                .GroupBy(pli => pli.ProductId)
                .Select(g => new {
                    ProductId = g.Key,
                    Product = g.First().Product,
                    Items = g.OrderBy(i => Model.ProductType.ProductTypeVariants.FirstOrDefault(v => v.Id == i.ProductTypeVariantId)?.DisplayOrder ?? 999).ToList()
                })
                .OrderBy(g => g.Product.Name)
                .ToList();

            <form asp-action="SaveAllVariantPrices" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="priceListId" value="@Model.Id" />

                <div class="table-responsive">
                    <table class="table table-bordered table-hover">
                        <thead class="table-light">
                            <tr>
                                <th>SKU</th>
                                <th>Producto</th>
                                @foreach (var variant in Model.ProductType.ProductTypeVariants.Where(v => v.IsActive).OrderBy(v => v.DisplayOrder))
                                {
                                    <th class="text-center">@variant.Name</th>
                                }
                                <th class="text-center">Descuento %</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var productGroup in productsGrouped)
                            {
                                @* Calcular el descuento actual del producto (asumiendo que todas las variantes tienen el mismo % de descuento) *@
                                var firstItem = productGroup.Items.FirstOrDefault();
                                var currentDiscountPercent = 0m;

                                if (firstItem != null)
                                {
                                    var activeDiscount = firstItem.GetActiveDiscount();
                                    if (activeDiscount != null && firstItem.Price > 0)
                                    {
                                        currentDiscountPercent = ((firstItem.Price - activeDiscount.DiscountedPrice) / firstItem.Price) * 100;
                                    }
                                }

                                <tr>
                                    <td><code>@productGroup.Product.SKU</code></td>
                                    <td><strong>@productGroup.Product.Name</strong></td>
                                    @foreach (var variant in Model.ProductType.ProductTypeVariants.Where(v => v.IsActive).OrderBy(v => v.DisplayOrder))
                                    {
                                        var variantItem = productGroup.Items.FirstOrDefault(i => i.ProductTypeVariantId == variant.Id);
                                        <td class="text-center">
                                            @if (variantItem != null)
                                            {
                                                @if (Model.StoreId.HasValue)
                                                {
                                                    <input type="number" value="@variantItem.Price" class="form-control form-control-sm text-center" readonly disabled title="Use el ajuste de porcentaje arriba" style="width: 100px; display: inline-block;" />
                                                }
                                                else
                                                {
                                                    <input type="hidden" name="itemIds[]" value="@variantItem.Id" />
                                                    <div class="input-group input-group-sm" style="width: 120px; margin: 0 auto;">
                                                        <span class="input-group-text">$</span>
                                                        <input type="number" name="prices[@variantItem.Id]" value="@variantItem.Price" class="form-control form-control-sm text-center" step="0.01" min="0" required />
                                                    </div>
                                                    @* Mostrar precio final si hay descuento *@
                                                    var finalPrice = variantItem.GetFinalPrice();
                                                    var hasDiscount = variantItem.HasDiscount();

                                                    if (hasDiscount)
                                                    {
                                                        <small class="text-success d-block mt-1">
                                                            <strong>Final: $@finalPrice.ToString("N2")</strong>
                                                        </small>
                                                    }
                                                }
                                            }
                                            else
                                            {
                                                <span class="text-muted">-</span>
                                            }
                                        </td>
                                    }
                                    <td class="text-center">
                                        @if (firstItem != null && !Model.StoreId.HasValue)
                                        {
                                            <div class="input-group input-group-sm" style="width: 100px; margin: 0 auto;">
                                                <input type="number"
                                                       class="form-control form-control-sm text-center discount-percent-input"
                                                       data-product-id="@productGroup.ProductId"
                                                       data-item-ids="@string.Join(",", productGroup.Items.Select(i => i.Id))"
                                                       value="@(currentDiscountPercent > 0 ? currentDiscountPercent.ToString("0.##") : "")"
                                                       placeholder="0"
                                                       step="0.01"
                                                       min="0"
                                                       max="100"
                                                       title="Descuento aplicado a todas las variantes" />
                                                <span class="input-group-text">%</span>
                                            </div>
                                        }
                                        else if (Model.StoreId.HasValue)
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                @if (!Model.StoreId.HasValue)
                {
                    <div class="mt-3">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-save"></i> Guardar Todos los Precios
                        </button>
                    </div>
                }
            </form>
        }
        else if (Model.ProductType?.HasHeadingStyles == true && Model.ProductType.ProductTypeHeadingStyles.Any())
        {
            var productsGrouped = Model.PriceListItems
                .GroupBy(pli => pli.ProductId)
                .Select(g => new {
                    ProductId = g.Key,
                    Product = g.First().Product,
                    Items = g.OrderBy(i => Model.ProductType.ProductTypeHeadingStyles.FirstOrDefault(h => h.Id == i.ProductTypeHeadingStyleId)?.DisplayOrder ?? 999).ToList()
                })
                .OrderBy(g => g.Product.Name)
                .ToList();

            <div class="alert alert-info">
                <i class="bi bi-info-circle"></i>
                Este tipo de producto usa <strong>Estilos de Cabecera</strong>.
                Haz clic en "Rangos" para definir los precios por dimensiones para cada combinación de producto y estilo.
            </div>

            <div class="table-responsive">
                <table class="table table-bordered table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>SKU</th>
                            <th>Producto</th>
                            @foreach (var headingStyle in Model.ProductType.ProductTypeHeadingStyles.Where(h => h.IsActive).OrderBy(h => h.DisplayOrder))
                            {
                                <th class="text-center">@headingStyle.Name</th>
                            }
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var productGroup in productsGrouped)
                        {
                            <tr>
                                <td><code>@productGroup.Product.SKU</code></td>
                                <td><strong>@productGroup.Product.Name</strong></td>
                                @foreach (var headingStyle in Model.ProductType.ProductTypeHeadingStyles.Where(h => h.IsActive).OrderBy(h => h.DisplayOrder))
                                {
                                    var headingStyleItem = productGroup.Items.FirstOrDefault(i => i.ProductTypeHeadingStyleId == headingStyle.Id);
                                    <td class="text-center">
                                        @if (headingStyleItem != null)
                                        {
                                            <div class="btn-group-vertical" role="group">
                                                <a asp-action="ManageCurtainPricing"
                                                    asp-route-itemId="@headingStyleItem.Id"
                                                    class="btn btn-sm @(headingStyleItem.CurtainPricingConfig?.PricingType == GrupoMad.Models.CurtainPricingType.Normal ? "btn-primary" : "btn-outline-secondary") mb-1"
                                                    title="Configurar precios normales (45 rangos de altura)">
                                                    <i class="bi bi-gear-fill"></i> Normal
                                                </a>
                                                <a asp-action="ManageSpecialCurtainPricing"
                                                    asp-route-itemId="@headingStyleItem.Id"
                                                    class="btn btn-sm @(headingStyleItem.CurtainPricingConfig?.PricingType == GrupoMad.Models.CurtainPricingType.Special ? "btn-success" : "btn-outline-secondary") mb-1"
                                                    title="Configurar precios especiales (6 rangos de altura)">
                                                    <i class="bi bi-gear-fill"></i> Especial
                                                </a>
                                            </div>


                                            @* Mostrar cantidad de rangos definidos si existen *@
                                            var rangeCountsByItem = ViewBag.RangeCountsByItem as Dictionary<int, int>;
                                            var rangeCount = rangeCountsByItem?.ContainsKey(headingStyleItem.Id) == true ? rangeCountsByItem[headingStyleItem.Id] : 0;

                                            @if (rangeCount > 0)
                                            {
                                                <br />
                                                <small class="text-success">
                                                    <i class="bi bi-check-circle-fill"></i> @rangeCount rango(s)
                                                </small>
                                            }
                                            else
                                            {
                                                <br />
                                                <small class="text-muted">
                                                    <i class="bi bi-exclamation-circle"></i> Sin rangos
                                                </small>
                                            }

                                            @* Mostrar si tiene configuración de cortinas y el tipo *@
                                            @if (headingStyleItem.CurtainPricingConfig != null)
                                            {
                                                <br />
                                                @if (headingStyleItem.CurtainPricingConfig.PricingType == GrupoMad.Models.CurtainPricingType.Normal)
                                                {
                                                    <span class="badge bg-info" title="45 rangos de altura">
                                                        <i class="bi bi-gear-fill"></i> Normal (45)
                                                    </span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-success" title="6 rangos de altura">
                                                        <i class="bi bi-lightning-fill"></i> Especial (6)
                                                    </span>
                                                }
                                            }
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                }
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>SKU</th>
                            <th>Producto</th>
                            <th>Tipo</th>
                            <th>Precio Base</th>
                            <th>Variante</th>
                            <th>Precio Final</th>
                            <th>Descuentos Activos</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.PriceListItems.OrderBy(p => p.Product.Name))
                        {
                        <tr>
                            <td><code>@item.Product.SKU</code></td>
                            <td><strong>@item.Product.Name</strong></td>
                            <td>
                                @if (item.Product.ProductType != null)
                                {
                                    @switch (item.Product.ProductType.PricingType)
                                    {
                                        case GrupoMad.Models.PricingType.PerSquareMeter:
                                            <span class="badge bg-info">m²</span>
                                            break;
                                        case GrupoMad.Models.PricingType.PerUnit:
                                            <span class="badge bg-success">Unidad</span>
                                            break;
                                        case GrupoMad.Models.PricingType.PerLinearMeter:
                                            <span class="badge bg-warning">Metro</span>
                                            break;
                                        case GrupoMad.Models.PricingType.PerRangeLength:
                                            <span class="badge bg-secondary">Rango Largo</span>
                                            break;
                                        case GrupoMad.Models.PricingType.PerRangeDimensions:
                                            <span class="badge bg-secondary">Rango Dim</span>
                                            break;
                                    }
                                }
                            </td>
                            <td>
                                @* No mostrar precio base para productos con precios por rangos *@
                                @if (item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeLength ||
                                     item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeDimensions)
                                {
                                    <span class="text-muted">
                                        <i class="bi bi-dash-circle"></i> Definir rangos
                                    </span>
                                }
                                else if (Model.StoreId.HasValue)
                                {
                                    @* Para listas de tienda: mostrar precio pero no permitir edición directa *@
                                    <div class="input-group input-group-sm" style="width: 150px;">
                                        <span class="input-group-text">$</span>
                                        <input type="number" value="@item.Price"
                                               class="form-control form-control-sm" step="0.01" min="0" readonly disabled
                                               title="Use el ajuste de porcentaje arriba para modificar los precios" />
                                    </div>
                                    <small class="text-muted">Desde lista global</small>
                                }
                                else
                                {
                                    @* Para listas globales: permitir edición directa *@
                                    <form asp-action="UpdateItem" method="post" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="itemId" value="@item.Id" />
                                        <input type="hidden" name="priceListId" value="@Model.Id" />
                                        <input type="hidden" name="variantId" value="@item.ProductTypeVariantId" />
                                        <div class="input-group input-group-sm" style="width: 150px;">
                                            <span class="input-group-text">$</span>
                                            <input type="number" name="price" value="@item.Price"
                                                   class="form-control form-control-sm" step="0.01" min="0" required />
                                            <button type="submit" class="btn btn-primary btn-sm">💾</button>
                                        </div>
                                    </form>
                                }
                            </td>
                            <td>
                                @* No mostrar variante para productos con precios por rangos *@
                                @if (item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeLength ||
                                     item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeDimensions)
                                {
                                    <span class="text-muted">-</span>
                                }
                                else
                                {
                                    @* Mostrar el nombre de la variante si existe *@
                                    @if (item.ProductTypeVariant != null)
                                    {
                                        <span class="badge bg-secondary">@item.ProductTypeVariant.Name</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                }
                            </td>
                            <td>
                                @* Para productos por rangos no aplica precio final *@
                                @if (item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeLength ||
                                     item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeDimensions)
                                {
                                    <span class="text-muted">Ver rangos</span>
                                }
                                else
                                {
                                    var finalPrice = item.GetFinalPrice();
                                    <strong class="text-primary">$@finalPrice.ToString("N2")</strong>
                                }
                            </td>
                            <td>
                                @* Para productos por rangos no aplica descuentos *@
                                @if (item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeLength ||
                                     item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeDimensions)
                                {
                                    <span class="text-muted">-</span>
                                }
                                else
                                {
                                    var activeDiscount = item.GetActiveDiscount();
                                    var discountApplied = item.GetDiscountApplied();

                                    if (activeDiscount != null && discountApplied > 0)
                                    {
                                        <span class="badge bg-success" title="Descuento vigente hasta @activeDiscount.ValidUntil.ToString("dd/MM/yyyy")">
                                            -$@discountApplied.ToString("N2")
                                            <small>(P@(activeDiscount.Priority))</small>
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                }
                            </td>
                            <td>
                                @* Botón de rangos para productos con tipo PerRangeLength o PerRangeDimensions *@
                                @if (item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeLength)
                                {
                                    <a asp-action="ManageRangesByLength" asp-route-itemId="@item.Id" class="btn btn-sm btn-info" title="Gestionar Rangos por Largo">
                                        <i class="bi bi-rulers"></i> Rangos
                                    </a>
                                }
                                else if (item.Product?.ProductType?.PricingType == GrupoMad.Models.PricingType.PerRangeDimensions)
                                {
                                    <a asp-action="ManageRangesByDimensionsMatrix" asp-route-itemId="@item.Id" class="btn btn-sm btn-info" title="Gestionar Rangos por Dimensiones">
                                        <i class="bi bi-bounding-box"></i> Rangos
                                    </a>
                                }
                                else
                                {
                                    @* Botón de descuentos solo para productos que no son por rangos *@
                                    <a asp-action="ManageDiscounts" asp-route-itemId="@item.Id" class="btn btn-sm btn-light" title="Gestionar Descuentos">
                                        <i class="bi bi-percent"></i> Descuentos
                                    </a>
                                }

                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    }
    else
    {
        <div class="alert alert-info">
            No hay productos en esta lista.
        </div>
    }
</div>

<hr />
<div>
    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-info">Ver Detalles</a>
    <a asp-action="Index" class="btn btn-secondary">Volver a la Lista</a>
</div>

@* Estilos para inputs modificados *@
<style>
    .price-input-modified {
        background-color: #fff3cd !important;
        border-color: #ffc107 !important;
        border-width: 2px !important;
        animation: highlight-pulse 0.3s ease;
    }

    @@keyframes highlight-pulse {
        0% { background-color: #fff; }
        50% { background-color: #ffffcc; }
        100% { background-color: #fff3cd; }
    }

    #unsavedChangesCounter {
        position: fixed;
        bottom: 20px;
        right: 20px;
        z-index: 1050;
        display: none;
        animation: slide-in 0.3s ease;
    }

    @@keyframes slide-in {
        from {
            transform: translateY(100px);
            opacity: 0;
        }
        to {
            transform: translateY(0);
            opacity: 1;
        }
    }
</style>

@* Contador de cambios sin guardar *@
<div id="unsavedChangesCounter" class="alert alert-warning shadow-lg" role="alert">
    <i class="bi bi-exclamation-triangle-fill"></i>
    <strong><span id="changesCount">0</span> cambio(s) sin guardar</strong>
</div>

@* Script para detectar cambios y advertir al cerrar *@
<script>
    (function() {
        let hasUnsavedChanges = false;
        let unsavedCount = 0;
        const originalValues = new Map();
        const counter = document.getElementById('unsavedChangesCounter');
        const countDisplay = document.getElementById('changesCount');

        // Función para actualizar el contador visual
        function updateCounter() {
            if (unsavedCount > 0) {
                countDisplay.textContent = unsavedCount;
                counter.style.display = 'block';
            } else {
                counter.style.display = 'none';
            }
        }

        // Detectar cambios en inputs de precios - Sección de variantes
        const variantPriceInputs = document.querySelectorAll('input[type="number"][name^="prices["]');
        variantPriceInputs.forEach(input => {
            // Guardar valor original
            originalValues.set(input, input.value);

            input.addEventListener('input', function() {
                const originalValue = originalValues.get(this);
                const currentValue = this.value;

                if (currentValue !== originalValue) {
                    // El valor cambió
                    if (!this.classList.contains('price-input-modified')) {
                        this.classList.add('price-input-modified');
                        unsavedCount++;
                        hasUnsavedChanges = true;
                        updateCounter();
                    }
                } else {
                    // El valor volvió al original
                    if (this.classList.contains('price-input-modified')) {
                        this.classList.remove('price-input-modified');
                        unsavedCount--;
                        hasUnsavedChanges = unsavedCount > 0;
                        updateCounter();
                    }
                }
            });
        });

        // Detectar cambios en inputs de precios - Sección sin variantes
        const standardPriceInputs = document.querySelectorAll('input[type="number"][name="price"]');
        standardPriceInputs.forEach(input => {
            // Guardar valor original
            originalValues.set(input, input.value);

            input.addEventListener('input', function() {
                const originalValue = originalValues.get(this);
                const currentValue = this.value;

                if (currentValue !== originalValue) {
                    if (!this.classList.contains('price-input-modified')) {
                        this.classList.add('price-input-modified');
                        unsavedCount++;
                        hasUnsavedChanges = true;
                        updateCounter();
                    }
                } else {
                    if (this.classList.contains('price-input-modified')) {
                        this.classList.remove('price-input-modified');
                        unsavedCount--;
                        hasUnsavedChanges = unsavedCount > 0;
                        updateCounter();
                    }
                }
            });
        });

        // Advertir al cerrar/recargar la página
        window.addEventListener('beforeunload', function(e) {
            if (hasUnsavedChanges) {
                e.preventDefault();
                e.returnValue = 'Tienes cambios sin guardar. ¿Estás seguro de que quieres salir?';
                return e.returnValue;
            }
        });

        // Resetear flag al enviar formularios
        const forms = document.querySelectorAll('form');
        forms.forEach(form => {
            form.addEventListener('submit', function() {
                hasUnsavedChanges = false;
                unsavedCount = 0;
            });
        });
    })();

    // ==================== Manejo de descuentos porcentuales para variantes ====================
    (function() {
        const discountInputs = document.querySelectorAll('.discount-percent-input');
        let discountTimers = new Map();

        discountInputs.forEach(input => {
            input.addEventListener('input', function() {
                const productId = this.dataset.productId;
                const itemIds = this.dataset.itemIds;
                const discountPercent = parseFloat(this.value) || 0;

                // Limpiar timer anterior si existe
                if (discountTimers.has(productId)) {
                    clearTimeout(discountTimers.get(productId));
                }

                // Crear nuevo timer para enviar después de 1 segundo de inactividad
                const timer = setTimeout(() => {
                    applyDiscount(itemIds, discountPercent, this);
                }, 1000);

                discountTimers.set(productId, timer);
            });
        });

        async function applyDiscount(itemIds, discountPercent, inputElement) {
            try {
                // Agregar indicador visual de guardando
                inputElement.classList.add('border-info');
                inputElement.style.backgroundColor = '#e7f3ff';

                const response = await fetch('@Url.Action("ApplyPercentageDiscountToVariants", "PriceList")', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/x-www-form-urlencoded',
                    },
                    body: `itemIds=${encodeURIComponent(itemIds)}&discountPercent=${encodeURIComponent(discountPercent)}`
                });

                const result = await response.json();

                if (result.success) {
                    // Mostrar éxito temporalmente
                    inputElement.classList.remove('border-info');
                    inputElement.classList.add('border-success');
                    inputElement.style.backgroundColor = '#d4edda';

                    // Recargar la página después de un breve delay para mostrar los precios actualizados
                    setTimeout(() => {
                        location.reload();
                    }, 500);
                } else {
                    // Mostrar error
                    inputElement.classList.remove('border-info');
                    inputElement.classList.add('border-danger');
                    inputElement.style.backgroundColor = '#f8d7da';
                    alert('Error: ' + result.message);

                    setTimeout(() => {
                        inputElement.classList.remove('border-danger');
                        inputElement.style.backgroundColor = '';
                    }, 3000);
                }
            } catch (error) {
                console.error('Error al aplicar descuento:', error);
                inputElement.classList.remove('border-info');
                inputElement.classList.add('border-danger');
                inputElement.style.backgroundColor = '#f8d7da';
                alert('Error al comunicarse con el servidor');

                setTimeout(() => {
                    inputElement.classList.remove('border-danger');
                    inputElement.style.backgroundColor = '';
                }, 3000);
            }
        }
    })();
</script>
